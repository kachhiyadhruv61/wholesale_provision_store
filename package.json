{
  "dependencies": {
    "@emotion/cache": "^11.14.0",
    "@emotion/react": "^11.14.0",
    "@mantine/styles": "^6.0.22"
  },
  "scripts": {
    "build": "npm ci --prefix Frontend --legacy-peer-deps && npm run build --prefix Frontend"
  }
}
